/* Empty-state label: centered, muted */
#empty-label {
    -unity-text-align: middle-center;
    color: rgba(180, 180, 180, 0.6);
    margin-top: 16px;
}

/* Nodes filtered out by the search field: grayed text */
.filtered-out {
    color: rgba(180, 180, 180, 0.5);
}
